# Phase 4 source import

This branch imports the reviewed `v0.2.0-alpha` Customer Service Platform source tree from the Phase 4 archive already stored in the repository. The temporary importer workflow expands the archive, validates required project files, removes binary delivery artifacts from the branch, and commits the unpacked source for normal Git review and CI.
