# Publishing workflow

Feature milestones are prepared on `agent/*` branches and reviewed through pull requests before merging to `main`. The `AI-project-SRV` repository is not modified by this project.
